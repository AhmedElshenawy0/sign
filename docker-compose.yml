services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: signup
      POSTGRES_PASSWORD: signup
      POSTGRES_DB: signup
    ports:
      - "5432:5432"
    volumes:
      - signup_pg:/var/lib/postgresql/data

volumes:
  signup_pg:
